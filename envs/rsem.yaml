# Ported from upstream envs/rsem.yaml (the conda.rna environment; name
# kept as clindet_rsem per upstream). Pins are the upstream minimums.
name: clindet_rsem
channels:
  - conda-forge
  - bioconda
dependencies:
  - rsem>=1.3.3
  - bowtie2>=2.5.4
  - samtools>=1.22.1
  - star>=2.7.11b
  - kallisto
  - salmon>=1.10.3
  - trust4>=1.1.5
