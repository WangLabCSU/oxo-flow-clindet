name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      # The merged single-entry workflow needs wildcard-scoped `when`
      # (Traitome/oxo-flow#187) and per-instance expand_inputs binding —
      # not yet in the latest release, so build the engine from main.
      - name: Install oxo-flow (main, cargo)
        run: |
          cargo install --git https://github.com/Traitome/oxo-flow oxo-flow-cli --locked --quiet
          oxo-flow --version

      # run.sh covers the run_type loop (default-wes / wgs / rna dry-runs).
      - name: Validate, lint, dry-run
        run: bash ./test/run.sh
